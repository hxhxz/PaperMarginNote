# Article Read Chrome 扩展功能设计

状态：已批准，进入开发  
版本：v4  
更新时间：2026-08-25

## 1. 产品定位

Article Read 是运行在 Chrome 原生 PDF 查看器旁的论文阅读辅助扩展。用户继续使用 Chrome 打开本地或网络 PDF，扩展通过约 400px 宽的 Side Panel 提供翻译、论文问答、背景检索、学习总结和知识文档能力。

本产品不替换 Chrome PDF 查看器，不提供内置 PDF.js 阅读界面。PDF.js 仅可作为后台文本、页码和目录解析器。

## 2. 本期范围

### 包含

- Chrome 原生 PDF 旁的扩展侧边栏。
- 本地 `file://` PDF 和网络 PDF。
- 选中文字翻译、解释和引用提问。
- 基于整篇论文或指定章节的连续问答。
- 结束学习后生成或更新 Markdown 知识文档。
- 知识文档版本历史、差异、恢复、编辑和 `.md` 下载。
- OpenAI 或 OpenAI 兼容接口配置。

### 不包含

- 扩展内置 PDF 阅读器。
- 替换或修改 Chrome PDF 查看器界面。
- 论文库、跨论文管理和知识图谱。
- 云同步、账号、团队协作和 OCR。
- 对 Chrome 当前滚动页或章节的无条件自动读取。

## 3. 页面结构

### 03 · Chrome PDF 论文助手侧边栏

Side Panel 宽度基准为 400px，包含：

- 以“论文问答”为标题和页面主功能，不设置独立的当前论文或分析范围信息卡。
- 顶部右侧统一使用 30px 图标按钮：`Settings` 配置 API、`FileText` 打开知识文档、`CircleStop` 结束本次学习。界面不显示“结束”文字，通过悬停提示和无障碍标签说明完整含义。
- 操作图标统一采用 Lucide SVG，禁止使用 Unicode 字符模拟齿轮、文档、关闭、发送或方向箭头。
- 中部以连续对话为主体，回答必须展示论文引用或外部来源。
- 不设置快捷提问按钮；章节总结、背景检索和价值分析均通过自然语言问题进入同一对话。
- 用户在左侧 Chrome PDF 中选中的原文，以可移除的引用附件进入输入框，而不是作为独立页面模块。
- 引用附件显示原文摘录，以及匹配成功后的章节和页码。
- 用户在引用附件下输入问题并发送，交互参考 ChatGPT 的“引用内容 + 继续提问”模式。

API 配置使用同尺寸的侧边栏状态，包含服务类型、Base URL、API Key、模型名称、连接测试和本地 PDF 权限提示。

### 05 · 知识文档管理

点击侧边栏中的“知识文档”后，在新的 Chrome 标签页打开扩展内部页面：

```js
chrome.tabs.create({
  url: chrome.runtime.getURL(`knowledge.html?paperId=${paperId}`),
});
```

页面包含：

- 当前论文的 Markdown 知识文档。
- 文档编辑与自动保存。
- 版本历史。
- 版本差异。
- 版本恢复。
- `.md` 文件下载。
- 人工编辑内容保护标记。

## 4. PDF 接入方式

### 网络 PDF

- 从当前标签页取得 PDF URL。
- 在获得对应 host permission 后，由扩展后台获取文件内容。
- 在后台解析目录、页码、文本块和论文元数据。

### 本地 PDF

- Manifest 声明 `file:///*` 的可选访问范围。
- 引导用户在扩展详情中开启“允许访问文件网址”。
- 使用 `chrome.extension.isAllowedFileSchemeAccess()` 检查授权状态。
- 未授权时保留选中文字相关功能，并提示用户授权后才能解析全文。

### Chrome PDF 查看器限制

- 不向 Chrome 内置 PDF 查看器注入 UI。
- 不依赖查看器内部 DOM、滚动位置或私有接口。
- 不承诺自动获得用户当前可见页码。

## 5. 当前章节与上下文定位

定位优先级：

1. 用户选中文字后，通过文本匹配在后台解析结果中定位页码和章节。
2. 使用 PDF 自带目录建立章节页码范围。
3. 使用标题编号、字体和版式特征推断章节结构。
4. 无可靠锚点时，沿用最近一次已确认章节。
5. 仍无法定位时，要求用户从章节选择器中手动选择。

引用附件必须显示定位状态：

- `自动定位`：匹配结果可信。
- `最近上下文`：沿用最近一次选中内容。
- `用户选择`：用户手动指定章节。
- `待确认`：无法可靠判断。

系统不得把推断章节表现为确定结果。

## 6. 选中文字与输入框引用

用户在 Chrome PDF 中选择文本后，通过扩展右键菜单执行：

- 翻译选中内容。
- 解释这段内容。
- 引用内容并提问。
- 定位所属章节。

Chrome Context Menus 的 `selectionText` 作为输入，结果显示在 Side Panel。由于不控制 PDF 查看器 DOM，不设计贴附原文的自定义浮窗。

当用户选择“引用内容并提问”时：

1. 自动打开或聚焦 Side Panel。
2. 将 `selectionText` 插入输入框上方的引用附件。
3. 后台尝试匹配论文文本块、页码和章节。
4. 用户在同一个输入框中补充问题。
5. 用户可以点击 `×` 移除引用，移除后问题按当前整篇论文上下文处理。

引用附件属于待发送消息的一部分；发送后，在用户消息气泡中继续显示引用原文，保证问答上下文可追溯。

## 7. 对话与检索

- 论文问答是侧边栏唯一主流程；章节总结、背景检索和价值分析均由用户自然语言发起。
- “外部知识”作为单次问题的检索开关，不是快捷问题。OpenAI 接口优先使用 Responses API 的 Web Search；兼容接口可降级为独立检索 API 或仅模型回答。
- 默认优先使用当前论文证据。
- 回答引用显示章节和页码；文本匹配成功后可展示原文片段。
- `检索背景`允许使用 OpenAlex、Crossref、Semantic Scholar、Wikipedia 和模型网页搜索。
- 论文证据、外部知识和模型推断必须使用不同视觉语义。
- 论文中没有充分依据时明确提示，不用模型常识冒充论文内容。

## 8. 学习结束与知识版本

知识文档不在每轮问答后实时改写。

流程如下：

1. 阅读和问答期间保存原始消息、引用、外部来源和临时会话摘要。
2. 用户点击“结束本次学习”。
3. 模型提取本次新增理解、关键结论、概念、疑问和关联知识。
4. 与当前 Markdown 知识文档按主题合并。
5. 合并成功后创建一个完整、可恢复的新版本。
6. 打开 05 知识文档页面并展示本次变更摘要。

合并失败时保留原版本，不产生半完成文档。

## 9. 数据存储

- `chrome.storage.local`：API 配置、功能开关和轻量偏好。
- IndexedDB：PDF 元数据、文本块、章节、对话、引用、Markdown 文档和版本快照。
- API Key 不进入 IndexedDB、日志、知识文档或导出文件。
- 论文使用内容指纹作为主要本地标识，辅以文件名、URL 和 DOI。

核心实体：

- `PaperDocument`
- `Section`
- `TextChunk`
- `ChatSession`
- `CitationAnchor`
- `KnowledgeDocument`
- `KnowledgeDocumentVersion`

## 10. Chrome 能力与权限

建议权限：

- `sidePanel`
- `contextMenus`
- `storage`
- `activeTab`
- `tabs`
- 按需申请的网络 PDF host permissions
- 用户手动开启的 `file:///*` 访问

Side Panel 由扩展工具栏图标或右键菜单打开。知识文档使用扩展内部新标签页，不依赖外部网站或后端页面。

## 11. 异常与降级

- 无本地文件权限：提示授权，只处理用户主动提供的选中文字。
- 扫描版 PDF：提示没有可提取文本，本期不提供 OCR。
- 章节识别低置信度：要求用户选择章节。
- 文本匹配失败：引用保留为“用户选中内容”，不伪造页码。
- API 未配置或连接失败：保留 PDF 辅助界面并引导配置。
- 外部检索失败：继续提供仅基于论文的回答。
- 版本合并失败：保留旧版本和完整会话。

## 12. 验收标准

- Chrome 原生 PDF 保持不变，扩展侧边栏可独立打开和关闭。
- 侧边栏以论文问答为主，不展示当前论文或分析范围信息卡。
- 页面不存在快捷问题按钮，所有能力进入同一条自然语言对话流。
- `CircleStop` 图标固定在侧边栏右上角，悬停提示为“结束本次学习”，点击后执行学习结束流程。
- 本地 PDF 未授权时有清晰、可操作的权限提示。
- 右键选中文字能触发翻译、解释或引用提问。
- PDF 选中文本能作为可移除的引用附件进入输入框，并随用户问题一起发送。
- 匹配成功时能显示章节和页码；失败时不会伪造定位。
- API 配置可保存并测试连接。
- “知识文档”能打开独立扩展标签页。
- “结束本次学习”会合并知识并创建新版本，而不是实时覆盖文档。
- 知识文档支持编辑、比较、恢复和下载 Markdown。
- UI 中不存在论文库和知识图谱入口。
